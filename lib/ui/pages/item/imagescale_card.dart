import 'package:flutter/material.dart';

class ImageScaleCardItem extends StatelessWidget {
  const ImageScaleCardItem({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}